# MLOps Sentiment Analysis – Configurazione ambiente
# Copia questo file a .env e modifica i valori per il tuo ambiente

# ============================================================================
# PORTE DEI SERVIZI
# ============================================================================
# FastAPI app port (servizio di inference)
APP_PORT=8000

# MLflow UI port (model registry)
MLFLOW_PORT=5000

# Airflow webserver port (orchestration)
AIRFLOW_PORT=8080

# Prometheus port (time-series DB)
PROM_PORT=9090

# Prometheus Pushgateway port (buffer metriche)
PUSHGATEWAY_PORT=9091

# Grafana port (dashboards)
GRAFANA_PORT=3000

# ============================================================================
# MODELLO
# ============================================================================
# URI del modello MLflow per il serving
# Se vuoto, la app fallback automaticamente su HuggingFace
# Esempi:
#   models:/Sentiment/Production  (dalla registry locale)
#   s3://bucket/model             (da S3, se configurato)
MODEL_URI=

# Nome del modello nel registry MLflow
# Usato da training/evaluation scripts
REGISTERED_MODEL_NAME=Sentiment

# ============================================================================
# AIRFLOW
# ============================================================================
# Numero di samples per la modalità dev/smoke (training rapido per test)
# Usato da train_smoke.py per addestrare un piccolo modello sklearn
SMOKE_N_SAMPLES=1

# ============================================================================
# NOTE
# ============================================================================
# - Tutti i valori di default sono sicuri per development
# - Per production, configura certificati SSL, credenziali Grafana, auth Airflow
# - Se le porte sono occupate, cambia i valori sopra (es. APP_PORT=9000)
